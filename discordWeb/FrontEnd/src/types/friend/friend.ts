export type onlinefriend = {
  friendId: string;
  userName: string;
  profilePhoto?: string;
};

export type FriendType = {
  userName: string;
  email: string;
  userId: string;
  friendId: string;
  profilePhoto?: string;
};
