export type onlinefriend = {
  friendId: string;
  userName: string;
};
export type friendReuestType = {
  otherPersonName: string;
  isSentByMe: boolean;
  requestId: number;
};
export type FriendType = {
  userName: string;
  email: string;
  userId: string;
  friendId: string;
};
